# Tuples are immutable (Cannot be changed after declaration) objects that can store elements of any data type.
# Declaration:
my_tuple = (1, 2, 3, 4)
# Tuple is the type of a class
print(type(my_tuple))
for t in my_tuple:
    print(t)